    import XCTest
    @testable import TitleBarViewController

    final class TitleBarViewControllerTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(TitleBarViewController().text, "Hello, World!")
        }
    }
